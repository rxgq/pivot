proc fib = (n: int): int {
    let a: int = 0;
    let b: int = 0;

    while b < n {
        let temp: int = a + b;
        a = b;
        b = temp;

        echo temp;
    }

    return a;
}

fib(10);